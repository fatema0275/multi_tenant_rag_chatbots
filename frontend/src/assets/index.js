/**
 * assets/index.js
 * Barrel file for static assets used in React components.
 * Import SVG/PNG assets here and re-export them so component
 * imports stay consistent when assets move.
 *
 * Usage:
 *   import { logoSvg } from '../assets';
 *
 * NOTE: Vite handles asset imports natively.
 * e.g. import logoUrl from './logo.svg';  → resolves to a URL string
 */

// No binary assets committed to source — the design uses Lucide icons
// and CSS-based visuals throughout, so no image files are required.
// Add asset imports here as the project grows.

export {};
