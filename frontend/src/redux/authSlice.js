/**
 * authSlice.js — re-export for redux/ directory structure.
 * Canonical implementation is in src/store/authSlice.js.
 */
export * from '../store/authSlice';
export { default } from '../store/authSlice';
