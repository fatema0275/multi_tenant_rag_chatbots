/**
 * ProtectedRoute.jsx — in routes/
 * Re-exports the canonical ProtectedRoute from components/.
 * Keeping this here satisfies the "routes/" directory in the required folder
 * structure while avoiding duplication of the implementation.
 */
export { default } from '../components/ProtectedRoute';
